/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig,
{
    images: {
      domains: ['media.rawg.io'],
    },
  };


module.exports = {
    images: {
      domains: ['media.rawg.io'],
    },
  };
  